# Tested with numpy 2.4.4, scipy 1.17.1, python-flint 0.8.0 on Python 3.12
# (the environment recorded in logs/environment_2026-06-23.txt).
# Lower bounds are conservative; newer releases are expected to work. The
# floors exclude every published security advisory for these packages
# (numpy CVE-2021-34141, fixed in 1.22; scipy CVE-2023-25399, fixed in 1.10.0).
numpy>=1.22
scipy>=1.10
# python-flint powers the exact finite-field (F_p) rank used by the saturation-
# onset gate (scripts/saturation_threshold.py, invoked by reproduce.py), the b=4
# decisive run (scripts/run_decisive_flint.py), and the b=5 deep checks
# (scripts/saturation_threshold.py --bd 5 15 / --bd 5 16). If it is not installed,
# reproduce.py SKIPS the onset gate with a clear notice and still exits 0 on the
# remaining checks; install it to run that stage and obtain the full verification.
python-flint>=0.9.0
